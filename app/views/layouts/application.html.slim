doctype html
html
  head
    title
      | MyGround
    meta[name="viewport" content="width=device-width,initial-scale=1"]
    = csrf_meta_tags
    = csp_meta_tag
    = stylesheet_pack_tag 'application', media: 'all'
    = javascript_pack_tag 'application'
  body
    - if !(current_page?(root_path) && current_user.nil?)
      header
        nav.navbar role="navigation" aria-label="main navigation"
          .navbar-brand
            .navbar-item
              .field class="is-grouped"
                .control
                  = link_to image_tag('logo.png'), root_path
                .control
                  = link_to image_tag('diaries.png'), root_path
                - if current_user
                  .control
                    = link_to image_tag('me.png'), diaries_path(current_user)
                - else
                  .control
                    = link_to image_tag('me.png'), new_user_session_path
                - if user_signed_in?
                  .control
                    = link_to t('button.log_out'), destroy_user_session_path, method: :delete
            a role="button" class="navbar-burger" aria-label="menu" aria-expanded="false"
              span aria-hidden="true"
              span aria-hidden="true"
              span aria-hidden="true"
    .notice
      = notice
    .alert
      = alert
    = yield
